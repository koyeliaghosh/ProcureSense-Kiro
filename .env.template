# ProcureSense Configuration Template
# Copy this file to .env and update the values for your environment

# ============================================================================
# DEPLOYMENT OPTION CONFIGURATION
# Choose ONE of the following deployment options and uncomment the relevant section
# ============================================================================

# ----------------------------------------------------------------------------
# Option 1: Local Ollama with Smaller Model (RECOMMENDED for space constraints)
# Requirements: 8GB+ RAM, 5GB+ storage
# ----------------------------------------------------------------------------
LLM_PROVIDER=ollama
OLLAMA_HOST=localhost:11434
OLLAMA_MODEL=llama3.1:8b
OLLAMA_TIMEOUT=30
OLLAMA_MAX_TOKENS=4096

# Context Budgets (tokens) - Adjusted for smaller model
CONTEXT_BUDGET_TOTAL=2000
CONTEXT_BUDGET_GPC=500
CONTEXT_BUDGET_DSC=500
CONTEXT_BUDGET_TSC=800
CONTEXT_BUDGET_ETC=200

# ----------------------------------------------------------------------------
# Option 2: Local Ollama with gpt-oss-20b (if you have 40GB+ storage)
# Requirements: 16GB+ RAM, 40GB+ storage
# ----------------------------------------------------------------------------
# LLM_PROVIDER=ollama
# OLLAMA_HOST=localhost:11434
# OLLAMA_MODEL=gpt-oss-20b
# OLLAMA_TIMEOUT=60
# OLLAMA_MAX_TOKENS=4096

# Context Budgets (tokens) - Full budget for larger model
# CONTEXT_BUDGET_TOTAL=4000
# CONTEXT_BUDGET_GPC=1000
# CONTEXT_BUDGET_DSC=1000
# CONTEXT_BUDGET_TSC=1600
# CONTEXT_BUDGET_ETC=400

# ----------------------------------------------------------------------------
# Option 3: Remote Ollama Server
# Requirements: Network access to remote Ollama instance
# ----------------------------------------------------------------------------
# LLM_PROVIDER=ollama
# OLLAMA_HOST=your-server:11434
# OLLAMA_MODEL=gpt-oss-20b
# OLLAMA_TIMEOUT=60
# OLLAMA_MAX_TOKENS=4096

# ----------------------------------------------------------------------------
# Option 4: OpenAI-Compatible API
# Requirements: API credentials and network access
# ----------------------------------------------------------------------------
# LLM_PROVIDER=openai
# OPENAI_API_BASE=https://api.openai.com/v1
# OPENAI_API_KEY=your-api-key-here
# OPENAI_MODEL=gpt-4
# OLLAMA_TIMEOUT=30
# OLLAMA_MAX_TOKENS=4096

# ============================================================================
# COMMON CONFIGURATION (Required for all deployment options)
# ============================================================================

# FastAPI Server Configuration
SERVER_HOST=localhost
SERVER_PORT=8000

# Enterprise Policy Configuration - CUSTOMIZE THESE FOR YOUR ORGANIZATION
# Prohibited clauses that should never appear in contracts
PROHIBITED_CLAUSES=liability_waiver,indemnification,unlimited_liability
# Required clauses that must be present in all contracts
REQUIRED_CLAUSES=warranty,data_protection,termination_rights
# Budget thresholds by category (category:amount format)
BUDGET_THRESHOLDS=software:50000,hardware:100000,services:25000

# Compliance Configuration
VARIANCE_THRESHOLD=0.15
AUTO_REVISION_ENABLED=true
AUDIT_LOGGING_ENABLED=true